apiVersion: v1
kind: Service
metadata:
  name: chatui
  namespace: chat
  labels:
    app: chatui
spec:
  selector:
    app: chatui
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP
